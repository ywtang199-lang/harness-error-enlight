---
name: harness-error-enlight
description: "在项目问题分级、反复失败、临时修复复发、排查偏离主线、验证范围失控，或维护 Harness 错误启发记录时使用。用第一性原理反问与按风险触发的对抗性审查决定处理深度、验证范围和停止条件。"
license: MIT
---

# Harness 错误启发

使每次处置推进用户目标，并使错误经验改变下一次决策。记录错误不等于必须立即治理；决定延期不等于可以标记已解决。

## 进入任务

- 确认当前目标、目标完成状态、授权范围及已有验收要求；按项目要求读取现有协作规则和项目记忆。
- 按现象、组件和触发条件检索项目错误记录，仅展开相关条目。历史根因是待核验线索，不是本次结论。
- 普通问题用几句话完成判断，不强制生成报告、评分表或完整审查流程。
- 用户只要求讨论、诊断或预览时，保持只读；技能调用不额外授权修复、持久记忆、发布、迁移或改变项目规则。已明确授权的工作不重复索取许可。
- 本技能指导问题处置决策，不替代专门的调试、迁移或安全流程。没有出现相关问题或用户未要求维护经验时，不把正常任务改造成全项目审计。

## 1. 判断投入

分别判断问题类别（内容、代码、环境、工具、流程、架构等）、实际影响与当前处理优先级。不要由类别直接推断严重程度。

沿用项目已有 P 级定义。没有定义时使用以下暂定含义，并给出一句证据：

| 级别 | 判据 | 默认行动 |
| --- | --- | --- |
| P0 | 正在造成严重、难恢复的损害，或已有具体证据表明即将发生此类损害 | 暂停受影响操作；在授权内优先止损，保留现场，再确定恢复路径 |
| P1 | 阻塞本次核心目标，且没有可接受的恢复路径 | 聚焦解除阻塞，选择最小充分修复 |
| P2 | 影响局部，有可接受替代路径或不阻塞本次目标 | 按复发成本、排查成本和当前目标决定有限处理或延期 |
| P3 | 无当前实质影响的改进机会 | 仅在相关时记录，不自动工程化 |

严重风险不因与主线无关而降级；环境问题也可以是 P1。证据不足时标记暂定，不编造影响。P 级、状态和行动是不同信息。

## 2. 第一性原理反问

在选择方案前作简短自查；在复发、无进展、扩大范围或提出新基础设施时重新检查。只把影响决策的结论告诉用户，不逐项盘问用户：

- 用户真正要恢复或交付的可观察结果是什么？
- 哪些是事实、哪些是推断，什么证据能推翻当前假设？
- 当前约束来自产品需求，还是偶然的实现和习惯？
- 不处理的实际代价是什么？是否有更小、可恢复且满足目标的办法？
- 此方案消除了触发机制，还是只改变了当前进程、缓存、会话或机器的临时状态？
- 接下来哪个检查会改变我们的决定？达到什么证据后应停止？

有证据支持的简单修复可以直接进行；无需为了形式追问到抽象的组织或架构根因。

## 3. 有限排查与处置

- 行动前简述已知现象、按可能性或影响排列的假设、最小低风险检查。
- 每次实验有明确假设和预期区分信号；保留关键尝试的结果及被否定原因。避免同时改变多个变量后把成功归因于其中一个。
- 同一失败条件未改变，不重复同一尝试。多次失败或无信息增益时，停止盲试，重估假设、依赖和投入；次数本身不能证明架构有问题。
- 经授权、次数有界且能区分间歇性故障的复现实验可以重复；应说明本次重复要获得什么证据。外部服务明确的重试窗口到达也属于条件变化，不用盲试禁令阻止合理恢复。
- 对长或不确定的排查给出适合当前风险的检查点和停止条件；不凭空规定所有任务统一时限。已有项目停滞规则优先。
- 临时恢复允许作为有意识的处置：说明有效条件、遗留风险和重新处理触发点。涉及数据或外部影响时遵守原有授权与安全要求。
- 低优先级恢复后是否继续查根因，按收益、成本和当前目标判断；没有永久默认“必须彻查”或“必须延期”。重要取舍无法推断时再问用户。
- 需要范围外重构或新基础设施时，先提出具体收益与成本；不得以解决当前小问题为由自行实施。

## 4. 对抗性审查

用户明确要求审查，或存在复发、互相矛盾的证据、高影响/难恢复操作、拟扩大范围、长期行为规则变更时，阅读 [审查方法](references/adversarial-review.md)。普通局部问题仅做简短反例自查。

审查要同时找漏修和过度处理。其结论必须关联证据、实际影响和最小修正，不能通过新增大量假设风险扩大任务。

## 5. 最小充分验证与停止

- 在执行前明确要支持的结论，以及原故障的最小复现和必要受影响边界。沿用项目必须通过的验收，不能借本技能跳过交付门槛。
- 选择仍包含真实故障机制的最窄验证：缓存/持久化问题可能需要重启或刷新；不得用模拟替身抹去导致故障的边界。
- 声称防复发时验证相关重新触发条件。一次成功只支持对应环境和范围的结论。
- 扩大测试必须能指出具体受影响关系、失败信号、风险证据或明确交付要求。测试中发现的无关问题单独分级，不自动接管。
- 必要检查通过且无新增证据后停止。仅新改动、新失败、环境变化影响结果有效性或明确门槛要求，才重跑相关检查；不反复全量测试来增加心理确定感。
- 分别报告：功能恢复情况、根因确认程度、防护验证情况。未验证项明确列出，不将写完记录或测试全绿等同于用户目标达成。

## 6. 让错误启发参与下次决策

在已有授权和项目规则内维护记录，不新建第二套错误库。项目要求已确认根因必须落盘时，小错误也按现有模板记录；详细程度与影响相称，不凭空补齐原因。

项目没有错误库时，先在本次汇报给出简短记录；用户已要求建立持久记录时，在项目约定位置创建一个 Markdown 文件即可，没有约定则使用项目根目录 `project_error_enlight.md`。不要求先建立完整 Harness 矩阵。记录最少包含日期/标识、情境与影响、已知事实与根因置信程度、关键尝试、处置与有效范围、验证证据、遗留风险与再次处理触发点；缺证据如实标记未知。

- 新根因新编号；同根因复发更新原记录并保留历史。仅现象相似、根因尚不确定时先关联，避免过早合并。
- 沿用现有模板，将下列信息融入适当字段：适用情境/检索词、关键失败尝试、临时办法限制、直接故障与根因证据、验收缺口、有效防护和重新处理条件。
- 复发时检查：历史条目未检索到、条件改变、措施未实施、措施未覆盖该路径，或旧根因判断错误。不要一律归咎“模型没遵守”。
- 修复、记录、规则提升是不同动作。只有明确、稳定、有证据且可复用的经验才建议提升为长期规则；一次偶发事件不自动变成所有任务的硬约束。
- 失效或被新证据推翻的经验应标注适用范围或被替代关系，保留事件历史；不能通过删掉旧失败来制造“已解决”。不存储凭证、原始敏感日志或与复盘无关的个人数据。

默认文件职责（使用项目实际路径，不强行创建缺失文件）：

| 文件 | 职责 |
| --- | --- |
| SKILL.md | 通用判断方法与按需流程 |
| AGENTS.md | 简短行为要求、权威与入口 |
| project_error_enlight.md | 具体事件、证据、尝试、复发与防护 |
| PROJECT_MEMORY.md | 用户确认的长期事实与决策 |
| CONTRIBUTING.md | 稳定操作方法及验收命令 |

保持单处权威、其他位置引用；遇到已有决定冲突时暂停冲突范围，说明影响。规则更新须符合用户授权，不自动改全局配置或同步 worktree。

收尾简报只保留：完成到哪一层、验证证据、错误记录编号（如有）、未解风险与返回主线的下一步。

## 维护本技能

关键决策规则修改后，用 [行为案例](references/behavior-cases.md) 做有限验证，检查是否同时避免漏修与过度工程化。原理参考及许可证见 [来源说明](references/sources.md)。不要将本技能描述为宿主强制门禁、后台监控或保证不复发的机制。
