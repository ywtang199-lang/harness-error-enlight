# 贡献与持续改进

欢迎提交去敏后的真实失败案例、规则修正或文档改进。目标是更好的处置决定与更少的无效工作。

## 报告问题

在 Issue 中说明：

- 用户目标、相关授权与必要验收。
- 宿主/模型（如已知）及技能提交版本。
- 当时可用的关键证据与代理实际决定。
- 期望决定、用户影响，以及现有规则为何没有奏效。

不要提交凭证、个人数据或完整私有会话。使用最短可理解的脱敏片段；无法复现时标明，不补造证据。

## 修改规则

1. 指出要改变的具体决定，优先修订已有规则。
2. 限制触发条件，检查是否冲突、是否扩大授权、是否增加不必要测试或流程。
3. 使用相关行为案例验证，并加入至少一个相邻反例：应处理的仍处理，应停止的仍停止。
4. 在 PR 中报告改动、验证方式、结果和未验证范围。新增有效场景时更新 `references/behavior-cases.md`。

对于行为规则，先给评估者场景输入和待评技能，让其独立给出处置，再与预期判据比较。不要把预期答案当作任务提示来获得表面通过。有条件时比较旧版与新版；自查须如实标注，不称为独立评估。

默认一轮聚焦审查；发现问题后只复核相关变化。普通编辑无需多代理。不要自动增加 CI、后台服务、扫描器或永久全局约束。

## 本地检查

- 阅读完整差异，检查 Markdown 相对链接、YAML 字段及中英文可读性。
- `git diff --check` 检查空白错误。
- 若安装了 Codex `skill-creator`，可运行其 `scripts/quick_validate.py` 并传入本技能目录；该工具需要 Python 与 PyYAML，只检查结构，不验证行为。
- 只运行本次规则影响的行为案例和相关反例；README 排版修改无需重跑所有案例。

仓库采用 MIT。保留上游声明；引入第三方内容时核验对应文件许可，并更新来源说明。不要假设公开可读就等同于允许复制。
